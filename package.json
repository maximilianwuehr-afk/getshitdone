{
  "name": "getshitdone",
  "version": "1.0.0",
  "description": "AI-powered research assistant for Obsidian",
  "main": "main.js",
  "scripts": {
    "generate-prompts": "node scripts/generate-prompts.js",
    "dev": "npm run generate-prompts && node esbuild.config.mjs",
    "build": "npm run generate-prompts && node esbuild.config.mjs production",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "Maximilian Wühr",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^2.1.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.0",
    "obsidian": "^1.4.0",
    "typescript": "^5.3.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
